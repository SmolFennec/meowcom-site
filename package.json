{
  "devDependencies": {
    "@parcel/transformer-sass": "2.11.0",
    "parcel": "^2.11.0",
    "rimraf": "^5.0.5",
    "sass": "^1.69.7"
  },
  "scripts": {
    "dev": "parcel index.html reviews/index.html --no-source-maps",
    "build": "rimraf dist && parcel build index.html reviews/index.html --no-source-maps"
  }
}
